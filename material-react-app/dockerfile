# material-react-app/Dockerfile - VERSÃO PARA DESENVOLVIMENTO

# 1. Comece com uma imagem base do Node.js v18
FROM node:18-alpine

# 2. Defina o diretório de trabalho dentro do contêiner
WORKDIR /app

# 3. Copie os arquivos de dependências primeiro (para aproveitar o cache do Docker)
COPY package*.json ./

# 4. Instale as dependências
RUN npm install

# 5. Copie o resto do código da sua aplicação
COPY . .

# 6. Exponha a porta 3000, que é onde o servidor de desenvolvimento do React roda
EXPOSE 3000

# 7. Defina o comando para iniciar a aplicação em modo de desenvolvimento
CMD ["npm", "start"]